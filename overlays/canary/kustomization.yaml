apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Canary overlay — 1 replica, v2 image (latest).
# Added live during the ApplicationSet demo scene to show zero-touch env creation.
namespace: gitops-canary
resources:
- ../../base
images:
- name: quay.io/redhatworkshops/welcome-php
  newTag: latest
